export enum Endpoints {
  authorize = "/oauth2/authorize",
  accesToken = "/oauth2/token",
  revokeToken = "/oauth2/revoke",
  user = "/helix/users",
}

export enum Scope {
  UserRead = "user_read",
}
